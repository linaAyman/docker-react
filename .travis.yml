language: generic 
sudo: required
services:
  - docker


 before_install:
  - docker build -t lina/docker-react -f Dockerfile.dev . 

 script:
  - docker run -e CI=true lina/docker-react npm run test
